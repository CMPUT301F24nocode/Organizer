<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/lucky_background" /> <!-- Background color of the overlay -->
    <corners android:radius="16dp" /> <!-- Corner radius for rounding the corners -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp" />
</shape>
